class InstalledApp {
  constructor(id, appName, apkDir, size, icon) {
    this.id = id;
    this.appName = appName;
    this.apkDir = apkDir;
    this.size = size;
    this.icon = icon;
  }
}

export default InstalledApp;
